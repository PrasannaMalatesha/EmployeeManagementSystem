/**
 * JWT filter, {@code UserDetailsService}, {@code PermissionEvaluator} for
 * subtree data-scoping, rate-limit filter, {@code AuditorAware}. The
 * mechanisms are here; authorization <em>rules</em> live with each feature.
 */
package com.malatesha.ems.security;
