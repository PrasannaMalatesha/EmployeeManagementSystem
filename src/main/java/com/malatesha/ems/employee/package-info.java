/**
 * Employees, departments, positions, hierarchy, temporal employment history.
 * Public API: {@code EmployeeService}. Internal {@code entity} and
 * {@code repository} packages are not importable by other modules — see
 * PRD §6 rule 3, enforced by {@code ArchitectureTest}.
 */
package com.malatesha.ems.employee;
