/**
 * Document metadata and presigned-URL brokering to object storage. Bytes
 * never transit the API — see Flow 6 and DR-011. Candidate for extraction
 * to its own service if that ever becomes justified.
 */
package com.malatesha.ems.document;
