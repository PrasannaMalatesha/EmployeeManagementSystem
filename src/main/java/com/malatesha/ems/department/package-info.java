/**
 * Departments and positions. Kept as a separate module even though closely
 * coupled to {@code employee} — its data is looked up cross-module often
 * enough that a clear service boundary is worth the split.
 */
package com.malatesha.ems.department;
